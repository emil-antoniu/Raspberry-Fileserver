The `setup.sh` should be run as the configured user, without `sudo`. All main installations and configurations should be covered by this script. 

Check each individual app's folder for additional notes.